# 指标管理系统 - 主题和权限管理功能说明

## 概述
本次更新为指标管理系统增加了完整的主题管理和权限控制系统,实现了多级权限管理。

## 主要功能

### 1. 用户角色体系
系统支持三种用户角色:

- **超级管理员 (SUPER_ADMIN)**
  - 可以创建和管理所有用户
  - 可以创建和维护所有主题
  - 可以管理所有指标
  - 可以分配主题管理员

- **主题管理员 (TOPIC_ADMIN)**
  - 由超级管理员分配
  - 可以创建和维护主题
  - 可以管理所属主题下的指标
  - 可以管理主题下的成员

- **普通用户 (USER)**
  - 只有查询权限
  - 可以查看指标和生成的SQL
  - 不能创建、编辑或删除指标

### 2. 主题管理
- 主题由管理员创建和维护
- 指标归属于特定主题
- 可以按主题筛选和查看指标
- 支持主题下的用户分配和管理

### 3. 权限控制
- 只有超级管理员和主题管理员可以创建/编辑/删除指标
- 普通用户只能查看指标
- 基于角色的访问控制(RBAC)
- Spring Security提供安全保障

## 默认用户账号

系统提供以下默认测试账号:

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 超级管理员 | root | root | 最高权限 |
| 主题管理员1 | admin1 | admin1 | 主题管理权限 |
| 主题管理员2 | admin2 | admin2 | 主题管理权限 |
| 普通用户1 | user1 | user1 | 只读权限 |
| 普通用户2 | user2 | user2 | 只读权限 |

## 数据库初始化

### 1. 执行SQL脚本
请执行以下SQL脚本创建所需的表结构:

```sql
-- 文件位置: src/main/resources/schema_with_auth.sql
```

该脚本会创建:
- `topic` - 主题表
- `sys_user` - 用户表
- `user_topic` - 用户主题关联表
- 修改 `metric_definition` 表,增加主题关联字段

### 2. 初始化默认用户

启动应用后,通过以下方式初始化默认用户:

**方法1: 调用初始化接口**
```bash
curl -X POST http://localhost:8080/init/users
```

**方法2: 使用Postman或其他HTTP工具**
- URL: `http://localhost:8080/init/users`
- Method: POST

初始化成功后,将创建5个默认用户(如上表所示)。

## 使用说明

### 1. 登录系统
- 访问: `http://localhost:8080/login.html`
- 使用上述默认账号登录
- 系统会自动跳转到指标管理页面

### 2. 指标管理页面 (index.html)
- **导航栏**
  - 显示当前登录用户信息
  - 主题筛选器(可按主题筛选指标)
  - 退出按钮
  - 超级管理员可访问"系统管理"链接

- **指标列表**
  - 显示指标所属主题
  - 根据用户角色显示/隐藏编辑和删除按钮
  - 普通用户只能查看和生成SQL

- **创建/编辑指标**
  - 需要选择所属主题
  - 只有管理员角色可见
  - 普通用户不显示表单区域

### 3. 系统管理页面 (admin.html)
**访问条件**: 仅超级管理员可访问

- **用户管理**
  - 创建、编辑、删除用户
  - 设置用户角色(超级管理员/主题管理员/普通用户)
  - 启用/禁用用户账号

- **主题管理**
  - 创建、编辑、删除主题
  - 为每个主题分配管理员和成员
  - 管理主题下的用户权限

### 4. 主题用户分配
在主题管理中,点击"分配用户"按钮:
- 选择要添加到主题的用户
- 设置用户在主题中的角色:
  - **管理员**: 可以管理该主题下的指标
  - **成员**: 只能查看该主题下的指标
- 可以随时移除用户

## 技术实现

### 后端技术栈
- **Spring Boot 3.2.0**
- **Spring Security** - 安全认证和授权
- **Spring Data JPA** - 数据持久化
- **PostgreSQL** - 数据库
- **BCrypt** - 密码加密

### 前端技术
- **纯HTML/CSS/JavaScript**
- **无框架依赖**
- **响应式设计**

### 主要新增文件

#### 实体类 (Entity)
- `SysUser.java` - 用户实体
- `Topic.java` - 主题实体
- `UserTopic.java` - 用户主题关联实体

#### Repository
- `SysUserRepository.java` - 用户数据访问
- `TopicRepository.java` - 主题数据访问
- `UserTopicRepository.java` - 用户主题关联数据访问

#### Service
- `UserService.java` - 用户管理业务逻辑
- `TopicService.java` - 主题管理业务逻辑

#### Controller
- `AuthController.java` - 认证相关(登录、登出、获取当前用户)
- `UserController.java` - 用户管理API
- `TopicController.java` - 主题管理API
- `InitController.java` - 初始化接口

#### 配置
- `SecurityConfig.java` - Spring Security安全配置

#### 前端页面
- `login.html` - 登录页面
- `admin.html` - 系统管理页面(用户和主题管理)
- `index.html` - 更新后的指标管理页面

## 权限流程

### 创建指标的权限流程
1. 用户登录系统
2. 系统检查用户角色
3. 如果是SUPER_ADMIN或TOPIC_ADMIN,显示表单
4. 用户填写指标信息,选择所属主题
5. 保存时,后端验证用户权限
6. 指标与主题关联保存

### 主题管理的权限流程
1. 超级管理员登录
2. 访问系统管理页面
3. 创建主题
4. 分配主题管理员
5. 主题管理员可以管理该主题下的指标

## 安全特性

1. **密码加密**: 使用BCrypt算法加密存储
2. **Session管理**: 无状态Session,适合RESTful API
3. **CSRF保护**: 已禁用(前后端分离场景)
4. **角色验证**: 基于Spring Security的角色检查
5. **权限控制**: 前端UI和后端API双重权限控制

## 注意事项

1. **初始化接口**: `/init/users` 接口仅供首次初始化使用,生产环境应删除或禁用
2. **密码安全**: 默认密码仅为测试使用,生产环境应强制修改
3. **权限分配**: 主题管理员只能管理被分配的主题,不能管理其他主题
4. **数据隔离**: 不同主题的数据相互隔离,通过topicId区分

## 后续扩展建议

1. **JWT Token认证**: 当前使用Session-based认证,可升级为JWT
2. **细粒度权限**: 可以增加更细粒度的权限控制(如字段级权限)
3. **审计日志**: 记录用户操作日志
4. **数据权限**: 实现行级数据权限控制
5. **多租户**: 支持多租户隔离

## 问题排查

### 无法登录
- 检查数据库是否正确初始化
- 确认用户是否已创建(调用/init/users)
- 查看后端日志确认认证流程

### 权限问题
- 检查用户角色是否正确
- 确认SecurityConfig配置
- 查看浏览器控制台的网络请求

### 主题筛选无效
- 确认指标已关联主题
- 检查API请求是否正确
- 查看数据库中的topic_id字段

## 联系支持

如有问题,请查看:
- 后端日志: 控制台输出
- 前端调试: 浏览器开发者工具
- 数据库: 检查表结构和数据
